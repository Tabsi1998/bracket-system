services:
  mongodb:
    container_name: tls-staging-mongodb

  backend:
    container_name: tls-staging-backend
    environment:
      APP_ENV: production
      DB_NAME: "${DB_NAME:-tls_arena_staging}"

  frontend:
    container_name: tls-staging-frontend
