[Unit]
Description=THE LION SQUAD encrypted backup
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/the-lion-squad
ExecStart=/usr/bin/bash scripts/backup.sh
User=root
UMask=0077
Nice=10
